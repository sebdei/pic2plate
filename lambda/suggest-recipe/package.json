{
  "name": "identify",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "deploy": "zip -r function.zip . && aws lambda update-function-code --function-name pic2plate-suggest-recipe --zip-file fileb://function.zip",
    "sam": "cd .. && sam local start-api --host 0.0.0.0 --env-vars .env.json",
    "start": "npm run sam",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.126",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "openai": "^4.16.2"
  }
}
